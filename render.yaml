services:
  - type: web
    name: yaqeen-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python services/yaqeen_ad_api_deploy.py
    envVars:
      - key: PORT
        value: "10000"
